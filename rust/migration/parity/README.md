# Contract / Parity Lane

Each migrated subsystem receives:

- donor fixture/version
- semantic requirements
- Rust implementation
- parity test
- differences explicitly classified as intentional or regression

The C++ implementation is an oracle, not an architecture template.
