#include "runtime/SandboxAcceptanceIISystem.h"
namespace subspace {
SandboxAcceptanceIIReport SandboxAcceptanceIISystem::Evaluate(const SandboxAcceptanceIIState& s) const {SandboxAcceptanceIIReport r;auto check=[&](bool ok,const char* name){if(ok)r.satisfied++;else r.missing.push_back(name);};check(s.combatOperational,"combat");check(s.damageConsequences,"damage");check(s.repairsConsumeResources,"repair");check(s.ewarOperational,"ewar");check(s.dronesOperational,"drones");check(s.fleetDoctrineOperational,"fleet doctrine");check(s.dynamicFactions,"dynamic factions");check(s.explorationOperational,"exploration");check(s.anomalousSpace,"anomalous space");check(s.hazardsOperational,"hazards");check(s.materialEconomy,"materials");check(s.logisticsAutomated,"logistics");check(s.stationEconomy,"station economy");check(s.orbitalInfrastructure,"orbital infrastructure");check(s.capitalConstruction,"capital construction");check(s.carrierOperations,"carrier operations");check(s.corporationProgression,"corporation progression");check(s.persistentUniverse,"persistent universe");r.passed=r.satisfied==r.required;return r;}
}
