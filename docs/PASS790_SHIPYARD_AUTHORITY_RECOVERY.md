# Pass790R2 - Shipyard Authority Recovery

This pass repairs the live Shipyard without restoring synthetic/legacy visuals.

- Certified `content/derived/greyoxide_shipyard_v07` content loads even when historical modular OBJ roots are absent.
- Runtime readiness requires actual certified Shipyard modules and generated showcase recipes.
- Standalone Shipyard previews the same one-foot profile-following shield surface used by the live ship runtime.
- XS/S/M/L/XL now feeds the existing class-eligibility authority before procedural Shipyard generation.
- Kitbash dependency readiness verifies every catalogued OBJ and metadata payload instead of trusting marker files alone.
- A CTest source gate prevents the Pass336 early-return regression from returning.

Deferred from this recovery pass: the deeper pattern/camouflage appearance-layer stack and a GPU/image-based visual acceptance test. Those should be added after the restored Shipyard is visually certified.
