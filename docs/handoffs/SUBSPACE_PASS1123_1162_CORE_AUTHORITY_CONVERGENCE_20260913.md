# Pass1123-1162 Test Handoff

After patch intake, run the project-owned Full Quality Gate.

Manual acceptance priorities:

1. Rotate the ship to several headings. In default View transform mode, dragging a selected part in a screen direction must remain screen-consistent rather than following ship facing.
2. Generate twice without Reroll: seed and deterministic request identity must remain unchanged. Reroll must explicitly change the seed.
3. Large classes may show a deterministic DRAFT while the topology-first generator is unfinished, but no generated module may return to giant 28x-style scale or a collapsed planar craft.
4. Make an edit, Undo, Redo, and confirm exact authored state returns. Repeat with a socket transform and a staged placement.
5. Open Forge / Project Tools. Project operations must come from `project.control.json`; PowerShell operations must preview/launch through `pwsh`, never Python. Confirmation-requiring publication operations stay PCC-governed.

Authoritative promotion remains the Windows internal PCC Full Gate.
