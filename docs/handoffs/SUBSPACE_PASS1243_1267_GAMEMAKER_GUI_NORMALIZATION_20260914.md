# Pass1243-1267 Test Handoff

1. Apply through normal root patch intake.
2. Run internal PCC option 1 Full Quality Gate.
3. Existing Shipyard visual/runtime behavior must remain intact; this tranche adds the professional shell state behind the current renderer.
4. Confirm all new ship_editor translation units build under the existing recursive CMake source glob.
5. Static certification must confirm Asset Browser search/tags/favorites/saved filters, universal F3 search groups, Activity Dock tabs, instance/definition Properties separation, construction guides, five primary workspaces and deterministic New Seed + Generate authority.
6. Next tranche may replace visible legacy `BuildControls()`/combined Outliner-Properties rendering with `ShipyardProfessionalShellSystem`; do not add more bespoke UI state to the legacy renderer first.
