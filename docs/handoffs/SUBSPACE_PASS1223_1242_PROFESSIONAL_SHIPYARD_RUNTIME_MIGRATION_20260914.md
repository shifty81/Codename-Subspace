# Pass1223-1242 Test Handoff

After intake, run the project-owned Full Quality Gate.

Acceptance:
1. Existing Shipyard behavior remains operational.
2. New migration/controller/panel/job sources compile in `subspace_engine`.
3. New Seed + Generate is one explicit seed change followed by the existing deterministic generator.
4. Outliner and Properties are separate stable-ID models.
5. Activity / Jobs is a shared state authority.
6. Test is not routed to Dev World.
