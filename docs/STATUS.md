# Codename Subspace Status

## Current source line

**Pass902–911 — Deep Project Alignment Normalization R1** on top of standalone PCC R6R2 + Pass892–901 editor dock foundation.

## Evidence

- Portable C++ configure/build: **PASS**.
- Registered CTest suite: **82/82 PASS** in the audit environment before/after the alignment edits.
- Windows standalone-PCC Full Gate: **REQUIRED before GREEN**.
- GitHub `main` baseline before local normalization: `1f714de6ea5c86ae45d38e01cd6167ca3dc6e5d3`.

## Highest-impact truths

1. Current project identity is the persistent embodied sandbox/strategy/RPG described in `PROJECT_VISION.md`.
2. Full 3D hierarchical simulation is authoritative; the August 2D/no-landing authority is superseded.
3. Normal space -> atmosphere -> planetary-surface traversal is seamless by design; current runtime landing/planet-streaming implementation is incomplete.
4. `VectorTravelSystem` is viable in-system-drive groundwork; old interstellar rail source is detached/superseded; physical jump-gate transit is still missing.
5. `GameData/` is canonical authored runtime/game data; `content/` is governed metadata/schema/provenance authority.
6. Engine ECS, `NativeRuntimeServices`, and `NativeGameApplication` currently split state ownership; runtime composition must converge before adding broad new simulation layers.
7. Pass892–901 establishes canonical dock-layout state, but native visible editor/Shipyard chrome/hit-testing still needs to consume that dock model.
8. Detached `home/`, `expedition/`, `roguelite/`, `travel/`, migration and alternate client source must not be reactivated wholesale. Reuse requires deliberate porting into current authority.

See `docs/audits/DEEP_PROJECT_ALIGNMENT_AUDIT_20260912.md` and the machine-readable maturity/build-graph matrices.
